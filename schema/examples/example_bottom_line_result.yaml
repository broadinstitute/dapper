# Illustrative DAPPER graph for a GWAS "bottom line" result: one Dataset
# generated by one Activity and backed by one manifest DrsObject. This is a
# reference document (a mixed set of node types), not a single-class
# instance; validate individual nodes with `linkml-validate -C <ClassName>`
# if desired.
#
# All values below are illustrative, not transcribed from a real study.
# There is no dedicated "bottom line" node class — a bottom-line result is
# just a Dataset, distinguished by its `name`/`description` like any other
# — and the multi-file Spark output is represented as a single
# manifest-backed DrsObject (has_drs_object) rather than as individual
# variant-level nodes. See
# notes/gptRecommendations/bottom-line-provenance-recommendations.md for why:
# an S3 prefix of per-partition Spark output is a multi-file dataset, so
# versioning is manifest-based, not single-object or row-level, and the
# doc's own recommended mapping reuses Dataset/Activity/DrsObject directly
# rather than introducing a new class.
#
# Regenerate after re-minting: see schema/examples/README.md
#
# Nothing here is transcribed from a real analysis run, so all 3 nodes are
# listed below.
_illustrative:
  - dapper:Activity.wJzaZQWxxACtrWYRP89EhNjzc5ZaXBSF
  - dapper:DrsObject.15vrw2UABhaPg2JROaFspKEOiJMFPIbe
  - dapper:Dataset.3_QblmXDJVn8WX9JZWuf7tVXDM8201F8

activities:
  - id: dapper:Activity.wJzaZQWxxACtrWYRP89EhNjzc5ZaXBSF
    name: LoadTransEthnicStage T2D
    activity_type: bottom-line-load-trans-ethnic
    command: "python loadAnalysis.py --trans-ethnic --phenotype T2D"
    entrypoint: loadAnalysis.py
    repo_url: https://github.com/broadinstitute/MethodsBioindex
    code_version: 4f2c9ab
    generated_at_time: "2026-08-14T12:00:00"

drs_objects:
  - id: dapper:DrsObject.15vrw2UABhaPg2JROaFspKEOiJMFPIbe
    drs_id: bottom-line-t2d-transethnic-v1
    self_uri: drs://bottom-line/bottom-line-t2d-transethnic-v1
    size: 123456789
    checksum_sha256: 3b1c9e4a5d6f7e8091a2b3c4d5e6f7081920a1b2c3d4e5f60718293a4b5c6d7
    mime_type: application/x-ndjson
    access_methods:
      - s3

datasets:
  - id: dapper:Dataset.3_QblmXDJVn8WX9JZWuf7tVXDM8201F8
    name: Bottom-line trans-ethnic meta-analysis for T2D
    version: "1"
    resource_type: dataset
    description: >-
      Final trans-ethnic bottom-line result for phenotype T2D — variant-level
      summary statistics from a genetic association meta-analysis.
    access_level: controlled

# ---- Reified provenance edges (the portal renders these, not inline fields) ----
was_generated_by_edges:
  - subject: dapper:Dataset.3_QblmXDJVn8WX9JZWuf7tVXDM8201F8
    predicate: prov:wasGeneratedBy
    object: dapper:Activity.wJzaZQWxxACtrWYRP89EhNjzc5ZaXBSF

has_drs_object_edges:
  - subject: dapper:Dataset.3_QblmXDJVn8WX9JZWuf7tVXDM8201F8
    predicate: dapper:hasDrsObject
    object: dapper:DrsObject.15vrw2UABhaPg2JROaFspKEOiJMFPIbe
