# A single valid NIH-DAPP `Dataset` node instance, adapted from George
# Papanicolaou's (NIH/OD) worked example. Validate with:
#
#   uv run --with linkml linkml-validate \
#     -s ../dapper.yaml -C Dataset example_dataset.yaml
#
# Cross-references are CURIEs pointing at other nodes' `id` (see
# example_graph.yaml for the full node set they resolve to).

# `id` is the computed content address; the DOI it was published under is
# preserved as an alternate_identifier rather than displaced by it.
id: dapper:Dataset.RZN4pby80ynDBrXyIfFE_a6vfKJa7NgN
alternate_identifier:
  - doi:10.12345/example.dataset.v2
name: Example NIH Multi-Omics Dataset
identifier_type: DOI
version: 2.1.0
resource_type: Dataset
publication_year: 2026
description: Curated multi-omics dataset generated by the Example NIH Consortium.
publisher: ror:0155zta11            # National Institutes of Health
access_level: public
controlled_access: false

# --- authoritative attribution (mirror_mutable: false) ---
has_creator:
  - orcid:0000-0002-1825-0097       # Jane A. Smith
  - ror:04abcde12                   # Example NIH Consortium
has_contributor:
  - orcid:0000-0003-1234-5678       # Alex R. Lee (DataCurator)
funded_by:
  - nih:award/U54-EXAMPLE-123456
is_described_by:
  - nih:publication/10.1000-example.publication
has_recommended_citation: nih:citation/example.dataset.v2

# --- license + provenance ---
has_license: nih:license/cc-by-4.0
was_generated_by: nih:activity/mcp-gateway-packaging-2026
was_derived_from:
  - nih:source/raw-sequencing
  - nih:source/release-manifest-v2.1

# --- 2.0 links ---
has_data_use_term:
  - nih:duo/DUO_0000042        # permission: general research use
  - nih:duo/DUO_0000019        # modifier: publication required
has_drs_object:
  - nih:drs/example-object-1
packaged_as: nih:rocrate/example.dataset.v2
has_workflow_provenance:
  - nih:bco/example-pipeline-v2

# --- mirror-appended (append-only; NOT authoritative) ---
has_mirror_provenance:
  - nih:mirror/openai-us-west-1-2026-07-22
