# A single GeneSet instance, EXTRACTED from example_geneset_graph.yaml.
#
# `funded_by` and `has_recommended_citation` were removed rather than left
# pointing at an Award and a RecommendedCitation that exist nowhere. See
# example_dataset.yaml for a resource that carries full NIH attribution — every
# id there resolves to a real object in example_graph.yaml.
#
#   uv run --with linkml linkml-validate \
#     -s schema/dapper.yaml -C GeneSet schema/examples/example_geneset.yaml

id: dapper:GeneSet.0dj0poPIUTC8EFQxG5p52jO554zJB6gZ
name: HuBMAP ASCT+B augmented marker gene-set library (model HZ2)
member_type: gene
n_genes: 1747
n_sets: 487
organism: human
genome_build: hg38
was_generated_by: dapper:Activity.CH9BwH64-PsjRM5UcETW5ooWQ1rZ1Fxa
was_derived_from:
- dapper:C2M2File.CXfIyIocwZWUCAERcmhJDfH8ZVtfVIF9
